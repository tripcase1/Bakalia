{
  "name": "al-hera-fresh-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/frontend",
    "apps/backend"
  ],
  "scripts": {
    "dev": "npm --prefix apps/frontend run dev",
    "dev:frontend": "npm --prefix apps/frontend run dev",
    "dev:backend": "npm --prefix apps/backend run start:dev",
    "build": "npm --prefix apps/frontend run build && npm --prefix apps/backend run build",
    "install:all": "npm --prefix apps/frontend install && npm --prefix apps/backend install"
  }
}
